# Modern, Python 3.10+ compatible, pinned, reproducible requirements (updated for detected Python 3.13+)
numpy==2.3.5
scikit-learn==1.7.2
pandas==2.3.3
matplotlib==3.10.7
scipy==1.16.3
pytest==9.0.1
fastapi==0.123.0
uvicorn==0.38.0
# NOTE: Do not pin pip/setuptools/wheel here; manage them separately for compatibility
# keep requirements minimal and pin package versions for reproducibility
